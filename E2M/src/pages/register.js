import Session from '../api/session';

const register = () => {
    return (
        <div className='Register'>
            <h1>Register</h1>
            <Session />
        </div>
    );
};

export default register